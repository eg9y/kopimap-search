{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "dotenv -e .env nitro dev",
    "prepare": "nitro prepare",
    "preview": "dotenv -e .env node .output/server/index.mjs",
    "start": "dotenv -e .env node .output/server/index.mjs"
  },
  "devDependencies": {
    "nitropack": "latest",
    "dotenv-cli": "^7.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.44.4",
    "meilisearch": "^0.41.0",
    "dotenv": "^16.3.1"
  }
}